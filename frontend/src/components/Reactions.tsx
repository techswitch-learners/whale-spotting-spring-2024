// import { useState } from "react"
// import { Button } from "react-bootstrap"
// import Reaction from "../models/view/Reaction"

// const emojis = {
//   LetsParty: "🤩",
//   NiceCatch: "😊",
//   SoSo: "😐",
//   Suspicious: "🤔",
// }

// interface ReactionProps {
//   reactions: Array<Reaction>
//   // sighting: Sighting
// }

//const Reactions = ({ reactions }: ReactionProps) => {
//{sighting} : ReactionProps
const Reactions = () => {
  // const [reactionType, setReactionType] = useState(0)
  // const [count, setCount] = useState()
  // const reactions: { "LetsParty": 3,
  // "NiceCatch": 2,
  // "SoSo": 1,
  // "Suspicious": 1}

  // const handleClick = (value: number) => {
  //   setReactionType(value)
  //   console.log(value)
  // }

  return (
    <>
      {/* <div>
        {reactions.map((reaction) => (
          <>
            <Button key={reaction.type} variant="light" onClick={() => handleClick(reaction.type)}>
              {emojis[reaction.type]}
            </Button>
            <span style={{ position: "relative", top: "0.75rem" }}>{reaction.count}</span>
            <span style={{ position: "relative", top: "0.75rem" }}>{sighting.reactions.}</span>
          </>
        ))}
      </div> */}
    </>
  )
}

export default Reactions
